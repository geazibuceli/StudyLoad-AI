{
  "name": "study-balance-ai",
  "version": "1.0.0",
  "description": "Explainable academic overload forecasting and preventive planning, with privacy by default.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test --experimental-test-isolation=none",
    "train": "node scripts/train-model.js",
    "model:check": "node scripts/train-model.js --check",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npm run lint && npm run format:check && node --check src/server.js && node --check public/app.js && node scripts/check-domain.js && node scripts/train-model.js --check && node --test --experimental-test-isolation=none"
  },
  "engines": {
    "node": ">=22"
  },
  "keywords": [
    "artificial-intelligence",
    "education",
    "explainable-ai",
    "machine-learning",
    "student-wellbeing",
    "sdg-3",
    "sdg-4"
  ],
  "author": "Geazi Junior Buceli da Silva",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "eslint": "^9.15.0",
    "globals": "^15.12.0",
    "prettier": "^3.4.2"
  }
}
